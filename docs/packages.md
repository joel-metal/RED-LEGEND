# Auto-generated TypeScript contract clients land here after running:
#   stellar scaffold build
#
# Each contract gets its own package, e.g.:
#   packages/red_vault/   ← typed client for the red_vault contract
#
# These are gitignored (see root .gitignore) because they are build artifacts.
# Regenerate with: stellar scaffold build --env testnet
